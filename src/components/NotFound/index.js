import React from "react";

const NotFound = () => (
  <div
    style={{
      display: "flex",
      justifyContent: "center",
      alignItems: "center",
      height: "100vh"
    }}
  >
    <h2 style={{ fontSize: "60px" }}>404 Page not found! </h2>
  </div>
);

export default NotFound;
